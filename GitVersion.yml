mode: Mainline
branches:
  main:
    regex: ^master$|^main$
    increment: Patch
    is-mainline: true
  develop:
    regex: ^dev(elop)?(ment)?$
    mode: ContinuousDeployment
    tag: beta
    increment: Minor
ignore:
  sha: []
merge-message-formats: {}
