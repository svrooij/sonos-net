# Sonos.Web specs writer

This small app runs the Sonos.Web application in a test server and generates the OpenAPI specification.

The generated specification is stored in the Sonos.Web project under `openapi.v1.json`.
